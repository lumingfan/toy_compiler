int main() {
  int arr[10], n = scan(arr);
  int i = 0;
  while (i < 10) {
    if (i < n) then {
      arr[i] = arr[i] + i;
    } else {
      arr[i] = arr[i - 1] + i;
    } end
    i = i + 1;
  }
  print(n, arr);
  return 0;
}
